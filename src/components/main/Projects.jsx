import React from 'react';

const Projects = () => {
  return (
    <div className="p-4 bg-[var(--bg-color)] text-[var(--text-color)] transition-colors duration-700 ease-in-out">
      <h2>Projects</h2>
      <p>List of projects...</p>
    </div>
  );
};

export default Projects;
